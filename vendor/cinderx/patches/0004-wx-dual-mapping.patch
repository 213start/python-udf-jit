diff --git a/cinderx/Jit/code_allocator.cpp b/cinderx/Jit/code_allocator.cpp
index ddec51f299a2eb882c037bd22213f518d88afc82..72ea63bc7c36b884d33c65cd58ce88d25793007d 100644
--- a/cinderx/Jit/code_allocator.cpp
+++ b/cinderx/Jit/code_allocator.cpp
@@ -6,11 +6,11 @@
 #include "cinderx/Jit/codegen/code_section.h"
 #include "cinderx/Jit/config.h"
 
-#ifdef WIN32
-#include <Windows.h>
-#include <memoryapi.h>
-#else
+#ifndef WIN32
 #include <sys/mman.h>
+#include <unistd.h>
+#else
+#include <process.h>
 #endif
 
 #include <cstring>
@@ -30,27 +30,34 @@ namespace {
 // 2MiB to match Linux's huge-page size.
 constexpr size_t kAllocSize = 1024 * 1024 * 2;
 
-// Allocate memory for JIT'd code.
-uint8_t* allocPages(size_t size) {
-#ifndef WIN32
-  void* res = mmap(
-      nullptr,
-      size,
-      PROT_EXEC | PROT_READ | PROT_WRITE,
-      MAP_PRIVATE | MAP_ANONYMOUS,
-      -1,
-      0);
-  JIT_CHECK(
-      res != MAP_FAILED,
-      "Failed to allocate {} bytes of memory for code",
-      size);
+uint64_t currentProcessId() {
+#ifdef WIN32
+  return static_cast<uint64_t>(_getpid());
 #else
-  void* res = VirtualAlloc(
-      nullptr, size, MEM_COMMIT | MEM_RESERVE, PAGE_EXECUTE_READWRITE);
-  JIT_CHECK(
-      res != nullptr, "Failed to allocate {} bytes of memory for code", size);
+  return static_cast<uint64_t>(getpid());
 #endif
-  return static_cast<uint8_t*>(res);
+}
+
+const asmjit::JitAllocator::CreateParams* dualMappingParams() {
+  static const asmjit::JitAllocator::CreateParams params = [] {
+    asmjit::JitAllocator::CreateParams value;
+    value.options = asmjit::JitAllocatorOptions::kUseDualMapping;
+    return value;
+  }();
+  return &params;
+}
+
+// Allocate memory for JIT'd code.
+asmjit::VirtMem::DualMapping allocPages(size_t size) {
+  asmjit::VirtMem::DualMapping mapping{};
+  asmjit::Error error = asmjit::VirtMem::allocDualMapping(
+      &mapping, size, asmjit::VirtMem::MemoryFlags::kAccessRWX);
+  JIT_CHECK(
+      error == asmjit::kErrorOk,
+      "Failed to allocate {} bytes of dual-mapped memory for code: {}",
+      size,
+      error);
+  return mapping;
 }
 
 bool setHugePages([[maybe_unused]] void* ptr, [[maybe_unused]] size_t size) {
@@ -88,9 +95,27 @@ ICodeAllocator* CodeAllocator::make() {
   return new CodeAllocator{};
 }
 
+CodeAllocator::CodeAllocator() {
+  ensureRuntimeForCurrentProcess();
+}
+
+void CodeAllocator::ensureRuntimeForCurrentProcess() const {
+  uint64_t process_id = currentProcessId();
+  if (
+      runtimes_.empty() ||
+      runtimes_.back().process_id != process_id) {
+    runtimes_.push_back(RuntimeGeneration{
+        process_id,
+        std::make_unique<asmjit::JitRuntime>(dualMappingParams()),
+    });
+  }
+}
+
 AllocateResult CodeAllocator::addCode(asmjit::CodeHolder* code) {
+  std::lock_guard lock{runtime_mutex_};
+  ensureRuntimeForCurrentProcess();
   void* addr = nullptr;
-  asmjit::Error error = runtime_.add(&addr, code);
+  asmjit::Error error = runtimes_.back().runtime->add(&addr, code);
 
   if (addr != nullptr && error == asmjit::kErrorOk) {
     used_bytes_.fetch_add(code->codeSize(), std::memory_order_relaxed);
@@ -100,27 +125,62 @@ AllocateResult CodeAllocator::addCode(asmjit::CodeHolder* code) {
 }
 
 asmjit::Error CodeAllocator::releaseCode(void* code) {
-  // Find the size of the allocated region.
-  asmjit::JitAllocator* inner = runtime_.allocator();
-  asmjit::JitAllocator::Span span;
-  if (auto error = inner->query(span, code); error != asmjit::kErrorOk) {
-    return error;
-  }
-
-  if (auto error = runtime_.release(code); error != asmjit::kErrorOk) {
-    return error;
+  std::lock_guard lock{runtime_mutex_};
+  for (RuntimeGeneration& generation : runtimes_) {
+    asmjit::JitAllocator::Span span;
+    if (
+        generation.runtime->allocator()->query(span, code) !=
+        asmjit::kErrorOk) {
+      continue;
+    }
+    if (
+        auto error = generation.runtime->release(code);
+        error != asmjit::kErrorOk) {
+      return error;
+    }
+    used_bytes_.fetch_sub(span.size(), std::memory_order_relaxed);
+    return asmjit::kErrorOk;
   }
-
-  used_bytes_.fetch_sub(span.size(), std::memory_order_relaxed);
-  return asmjit::kErrorOk;
+  return asmjit::kErrorInvalidArgument;
 }
 
 bool CodeAllocator::contains(const void* ptr) const {
-  asmjit::JitAllocator::Span unused;
+  std::lock_guard lock{runtime_mutex_};
   // asmjit docs don't say that query() is thread-safe, but peeking at the
   // implementation shows that it is.
-  return runtime_.allocator()->query(unused, const_cast<void*>(ptr)) ==
-      asmjit::kErrorOk;
+  for (const RuntimeGeneration& generation : runtimes_) {
+    asmjit::JitAllocator::Span unused;
+    if (
+        generation.runtime->allocator()->query(
+            unused, const_cast<void*>(ptr)) == asmjit::kErrorOk) {
+      return true;
+    }
+  }
+  return false;
+}
+
+void* CodeAllocator::writableAddress(const void* executable) const {
+  std::lock_guard lock{runtime_mutex_};
+  ensureRuntimeForCurrentProcess();
+  uint64_t process_id = currentProcessId();
+  for (const RuntimeGeneration& generation : runtimes_) {
+    asmjit::JitAllocator::Span span;
+    if (
+        generation.runtime->allocator()->query(
+            span, const_cast<void*>(executable)) != asmjit::kErrorOk) {
+      continue;
+    }
+    if (
+        generation.process_id != process_id ||
+        span.rw() == nullptr ||
+        span.rw() == span.rx()) {
+      return nullptr;
+    }
+    auto offset = static_cast<const uint8_t*>(executable) -
+        static_cast<const uint8_t*>(span.rx());
+    return static_cast<uint8_t*>(span.rw()) + offset;
+  }
+  return nullptr;
 }
 
 size_t CodeAllocator::usedBytes() const {
@@ -128,22 +188,23 @@ size_t CodeAllocator::usedBytes() const {
 }
 
 const asmjit::Environment& CodeAllocator::asmJitEnvironment() const {
-  return runtime_.environment();
+  std::lock_guard lock{runtime_mutex_};
+  ensureRuntimeForCurrentProcess();
+  return runtimes_.back().runtime->environment();
 }
 
 CodeAllocatorCinder::~CodeAllocatorCinder() {
-  for (std::span<uint8_t> alloc : allocations_) {
-#ifndef WIN32
+  for (Allocation& alloc : allocations_) {
     JIT_CHECK(
-        munmap(alloc.data(), alloc.size()) == 0, "Freeing code memory failed");
-#else
-    VirtualFree(alloc.data(), 0, MEM_RELEASE);
-#endif
+        asmjit::VirtMem::releaseDualMapping(
+            &alloc.mapping, alloc.size) == asmjit::kErrorOk,
+        "Freeing dual-mapped code memory failed");
   }
 }
 
 void CodeAllocatorCinder::ensureSpace(
-    uint8_t*& alloc,
+    uint8_t*& rx_alloc,
+    uint8_t*& rw_alloc,
     size_t& alloc_free,
     size_t size,
     bool use_huge_pages) {
@@ -154,14 +215,16 @@ void CodeAllocatorCinder::ensureSpace(
   lost_bytes_.fetch_add(alloc_free, std::memory_order_relaxed);
 
   size_t chunk_size = ((size / kAllocSize) + 1) * kAllocSize;
-  uint8_t* res = allocPages(chunk_size);
-  if (use_huge_pages && setHugePages(res, chunk_size)) {
+  asmjit::VirtMem::DualMapping mapping = allocPages(chunk_size);
+  if (use_huge_pages && setHugePages(mapping.rx, chunk_size)) {
     huge_allocs_.fetch_add(1, std::memory_order_relaxed);
   } else {
     fragmented_allocs_.fetch_add(1, std::memory_order_relaxed);
   }
-  alloc = res;
-  allocations_.emplace_back(res, chunk_size);
+  rx_alloc = static_cast<uint8_t*>(mapping.rx);
+  rw_alloc = static_cast<uint8_t*>(mapping.rw);
+  allocations_.push_back(
+      Allocation{mapping, chunk_size, currentProcessId()});
   alloc_free = chunk_size;
 }
 
@@ -184,22 +247,25 @@ void CodeAllocatorCinder::ensureSplitSpace(
 
   size_t total_needed = hot_needed + cold_needed;
   size_t chunk_size = ((total_needed / kAllocSize) + 1) * kAllocSize;
-  uint8_t* res = allocPages(chunk_size);
-  if (setHugePages(res, chunk_size)) {
+  asmjit::VirtMem::DualMapping mapping = allocPages(chunk_size);
+  if (setHugePages(mapping.rx, chunk_size)) {
     huge_allocs_.fetch_add(1, std::memory_order_relaxed);
   } else {
     fragmented_allocs_.fetch_add(1, std::memory_order_relaxed);
   }
-  allocations_.emplace_back(res, chunk_size);
+  allocations_.push_back(
+      Allocation{mapping, chunk_size, currentProcessId()});
 
   // Hot code grows forward from the start, cold code grows forward from a
   // split point. Split proportionally so each side gets at least what it
   // requested, distributing any surplus evenly.
   size_t surplus = chunk_size - total_needed;
   size_t hot_share = hot_needed + surplus / 2;
-  hot_alloc_ = res;
+  hot_rx_alloc_ = static_cast<uint8_t*>(mapping.rx);
+  hot_rw_alloc_ = static_cast<uint8_t*>(mapping.rw);
   hot_alloc_free_ = hot_share;
-  cold_alloc_ = res + hot_share;
+  cold_rx_alloc_ = hot_rx_alloc_ + hot_share;
+  cold_rw_alloc_ = hot_rw_alloc_ + hot_share;
   cold_alloc_free_ = chunk_size - hot_share;
 }
 
@@ -225,9 +291,11 @@ AllocateResult CodeAllocatorCinder::addSplitCode(asmjit::CodeHolder* code) {
 #else
   // On x86-64, RIP-relative addressing has a ±2GB range which is large enough
   // that independent allocations are unlikely to exceed it in practice.
-  ensureSpace(hot_alloc_, hot_alloc_free_, hot_size, true);
   ensureSpace(
-      cold_alloc_,
+      hot_rx_alloc_, hot_rw_alloc_, hot_alloc_free_, hot_size, true);
+  ensureSpace(
+      cold_rx_alloc_,
+      cold_rw_alloc_,
       cold_alloc_free_,
       cold_size,
       getConfig().cold_code_huge_pages);
@@ -237,7 +305,8 @@ AllocateResult CodeAllocatorCinder::addSplitCode(asmjit::CodeHolder* code) {
   // All offsets are relative to the hot allocation base so that asmjit can
   // resolve cross-section jumps correctly.
   size_t hot_offset = 0;
-  size_t cold_offset = static_cast<size_t>(cold_alloc_ - hot_alloc_);
+  size_t cold_offset =
+      static_cast<size_t>(cold_rx_alloc_ - hot_rx_alloc_);
   for (asmjit::Section* section : code->sections()) {
     CodeSection cs = codeSectionFromName(section->name());
     if (cs == CodeSection::kCold) {
@@ -250,9 +319,9 @@ AllocateResult CodeAllocatorCinder::addSplitCode(asmjit::CodeHolder* code) {
   }
 
   PROPAGATE_ERROR(code->resolveUnresolvedLinks());
-  PROPAGATE_ERROR(code->relocateToBase(uintptr_t(hot_alloc_)));
+  PROPAGATE_ERROR(code->relocateToBase(uintptr_t(hot_rx_alloc_)));
 
-  void* addr = hot_alloc_;
+  void* addr = hot_rx_alloc_;
 
   // Copy each section's data to the appropriate allocation.
   size_t total_size = 0;
@@ -263,14 +332,16 @@ AllocateResult CodeAllocatorCinder::addSplitCode(asmjit::CodeHolder* code) {
     }
     CodeSection cs = codeSectionFromName(section->name());
     if (cs == CodeSection::kCold) {
-      std::memcpy(cold_alloc_, section->data(), buffer_size);
-      flushInstructionCache(cold_alloc_, buffer_size);
-      cold_alloc_ += buffer_size;
+      std::memcpy(cold_rw_alloc_, section->data(), buffer_size);
+      flushInstructionCache(cold_rx_alloc_, buffer_size);
+      cold_rx_alloc_ += buffer_size;
+      cold_rw_alloc_ += buffer_size;
       cold_alloc_free_ -= buffer_size;
     } else {
-      std::memcpy(hot_alloc_, section->data(), buffer_size);
-      flushInstructionCache(hot_alloc_, buffer_size);
-      hot_alloc_ += buffer_size;
+      std::memcpy(hot_rw_alloc_, section->data(), buffer_size);
+      flushInstructionCache(hot_rx_alloc_, buffer_size);
+      hot_rx_alloc_ += buffer_size;
+      hot_rw_alloc_ += buffer_size;
       hot_alloc_free_ -= buffer_size;
     }
     total_size += buffer_size;
@@ -282,6 +353,7 @@ AllocateResult CodeAllocatorCinder::addSplitCode(asmjit::CodeHolder* code) {
 
 AllocateResult CodeAllocatorCinder::addCode(asmjit::CodeHolder* code) {
   std::lock_guard lock{allocator_mutex_};
+  ensureProcessGeneration();
 
   if (getConfig().multiple_code_sections) {
     return addSplitCode(code);
@@ -291,9 +363,14 @@ AllocateResult CodeAllocatorCinder::addCode(asmjit::CodeHolder* code) {
   PROPAGATE_ERROR(code->resolveUnresolvedLinks());
 
   size_t max_code_size = code->codeSize();
-  ensureSpace(hot_alloc_, hot_alloc_free_, max_code_size, true);
+  ensureSpace(
+      hot_rx_alloc_,
+      hot_rw_alloc_,
+      hot_alloc_free_,
+      max_code_size,
+      true);
 
-  PROPAGATE_ERROR(code->relocateToBase(uintptr_t(hot_alloc_)));
+  PROPAGATE_ERROR(code->relocateToBase(uintptr_t(hot_rx_alloc_)));
 
   size_t actual_code_size = code->codeSize();
   JIT_CHECK(actual_code_size <= max_code_size, "Code grew during relocation");
@@ -305,26 +382,43 @@ AllocateResult CodeAllocatorCinder::addCode(asmjit::CodeHolder* code) {
 
     JIT_CHECK(
         offset + buffer_size <= actual_code_size, "Inconsistent code size");
-    std::memcpy(hot_alloc_ + offset, section->data(), buffer_size);
+    std::memcpy(hot_rw_alloc_ + offset, section->data(), buffer_size);
 
     if (virtual_size > buffer_size) {
       JIT_CHECK(
           offset + virtual_size <= actual_code_size, "Inconsistent code size");
       std::memset(
-          hot_alloc_ + offset + buffer_size, 0, virtual_size - buffer_size);
+          hot_rw_alloc_ + offset + buffer_size,
+          0,
+          virtual_size - buffer_size);
     }
   }
 
-  void* addr = hot_alloc_;
+  void* addr = hot_rx_alloc_;
   flushInstructionCache(addr, actual_code_size);
 
-  hot_alloc_ += actual_code_size;
+  hot_rx_alloc_ += actual_code_size;
+  hot_rw_alloc_ += actual_code_size;
   hot_alloc_free_ -= actual_code_size;
   used_bytes_.fetch_add(actual_code_size, std::memory_order_relaxed);
 
   return AllocateResult{addr, asmjit::kErrorOk};
 }
 
+void CodeAllocatorCinder::ensureProcessGeneration() {
+  uint64_t process_id = currentProcessId();
+  if (process_id_ == process_id) {
+    return;
+  }
+  process_id_ = process_id;
+  hot_rx_alloc_ = nullptr;
+  hot_rw_alloc_ = nullptr;
+  hot_alloc_free_ = 0;
+  cold_rx_alloc_ = nullptr;
+  cold_rw_alloc_ = nullptr;
+  cold_alloc_free_ = 0;
+}
+
 asmjit::Error CodeAllocatorCinder::releaseCode([[maybe_unused]] void* code) {
   // TODO(T233607793): Actually implement deallocating memory.
   return asmjit::kErrorOk;
@@ -332,12 +426,33 @@ asmjit::Error CodeAllocatorCinder::releaseCode([[maybe_unused]] void* code) {
 
 bool CodeAllocatorCinder::contains(const void* ptr) const {
   std::lock_guard lock{allocator_mutex_};
-  for (std::span<uint8_t> alloc : allocations_) {
-    if (alloc.data() <= ptr && ptr < alloc.data() + alloc.size()) {
+  auto* address = static_cast<const uint8_t*>(ptr);
+  for (const Allocation& alloc : allocations_) {
+    auto* rx = static_cast<const uint8_t*>(alloc.mapping.rx);
+    if (rx <= address && address < rx + alloc.size) {
       return true;
     }
   }
   return false;
 }
 
+void* CodeAllocatorCinder::writableAddress(const void* executable) const {
+  std::lock_guard lock{allocator_mutex_};
+  auto* address = static_cast<const uint8_t*>(executable);
+  for (const Allocation& alloc : allocations_) {
+    auto* rx = static_cast<const uint8_t*>(alloc.mapping.rx);
+    if (rx <= address && address < rx + alloc.size) {
+      if (alloc.mapping.rw == nullptr || alloc.mapping.rw == alloc.mapping.rx) {
+        return nullptr;
+      }
+      if (alloc.process_id != currentProcessId()) {
+        return nullptr;
+      }
+      auto offset = address - rx;
+      return static_cast<uint8_t*>(alloc.mapping.rw) + offset;
+    }
+  }
+  return nullptr;
+}
+
 } // namespace jit
diff --git a/cinderx/Jit/code_allocator.h b/cinderx/Jit/code_allocator.h
index 013d0f7994150415f14c309788149cf0eb35474d..be0ae45f3e721a8c018946a39e203ae511c1f2a9 100644
--- a/cinderx/Jit/code_allocator.h
+++ b/cinderx/Jit/code_allocator.h
@@ -7,8 +7,9 @@
 #include <asmjit/asmjit.h>
 
 #include <atomic>
+#include <cstdint>
+#include <memory>
 #include <mutex>
-#include <span>
 #include <vector>
 
 namespace jit {
@@ -28,6 +29,7 @@ namespace jit {
 */
 class CodeAllocator : public ICodeAllocator {
  public:
+  CodeAllocator();
   ~CodeAllocator() override = default;
 
   // To be called once by JIT initialization after enough configuration has been
@@ -37,11 +39,20 @@ class CodeAllocator : public ICodeAllocator {
   AllocateResult addCode(asmjit::CodeHolder* code) override;
   asmjit::Error releaseCode(void* code) override;
   bool contains(const void* ptr) const override;
+  void* writableAddress(const void* executable) const override;
   size_t usedBytes() const override;
   const asmjit::Environment& asmJitEnvironment() const override;
 
  protected:
-  asmjit::JitRuntime runtime_;
+  struct RuntimeGeneration {
+    uint64_t process_id;
+    std::unique_ptr<asmjit::JitRuntime> runtime;
+  };
+
+  void ensureRuntimeForCurrentProcess() const;
+
+  mutable std::mutex runtime_mutex_;
+  mutable std::vector<RuntimeGeneration> runtimes_;
   std::atomic<size_t> used_bytes_{0};
 };
 
@@ -69,8 +80,15 @@ class CodeAllocatorCinder : public CodeAllocator {
   AllocateResult addCode(asmjit::CodeHolder* code) override;
   asmjit::Error releaseCode(void* code) override;
   bool contains(const void* ptr) const override;
+  void* writableAddress(const void* executable) const override;
 
  private:
+  struct Allocation {
+    asmjit::VirtMem::DualMapping mapping;
+    size_t size;
+    uint64_t process_id;
+  };
+
   // Add code with hot/cold section splitting. Called by addCode() when
   // multiple_code_sections is enabled. Caller must hold allocator_mutex_.
   AllocateResult addSplitCode(asmjit::CodeHolder* code);
@@ -78,7 +96,8 @@ class CodeAllocatorCinder : public CodeAllocator {
   // Ensure the given bump allocator has at least `size` bytes free, allocating
   // a new chunk if necessary.
   void ensureSpace(
-      uint8_t*& alloc,
+      uint8_t*& rx_alloc,
+      uint8_t*& rw_alloc,
       size_t& alloc_free,
       size_t size,
       bool use_huge_pages);
@@ -90,20 +109,27 @@ class CodeAllocatorCinder : public CodeAllocator {
   // (±128MB for B/BL, ±1MB for B.cond). Used only on aarch64.
   void ensureSplitSpace(size_t hot_needed, size_t cold_needed);
 
+  // Stop allocating into inherited shared backing pages after fork().
+  void ensureProcessGeneration();
+
   // Protects all allocator-owned state used by addCode()/contains().
   mutable std::mutex allocator_mutex_;
 
   // List of all chunks allocated, for use in deallocation and contains().
-  std::vector<std::span<uint8_t>> allocations_;
+  std::vector<Allocation> allocations_;
 
   // Hot code allocation state.
-  uint8_t* hot_alloc_{nullptr};
+  uint8_t* hot_rx_alloc_{nullptr};
+  uint8_t* hot_rw_alloc_{nullptr};
   size_t hot_alloc_free_{0};
 
   // Cold code allocation state (used when multiple code sections are enabled).
-  uint8_t* cold_alloc_{nullptr};
+  uint8_t* cold_rx_alloc_{nullptr};
+  uint8_t* cold_rw_alloc_{nullptr};
   size_t cold_alloc_free_{0};
 
+  uint64_t process_id_{0};
+
   // Number of bytes in total lost when allocations didn't fit neatly into
   // the bytes remaining in a chunk so a new one was allocated.
   std::atomic<size_t> lost_bytes_{0};
diff --git a/cinderx/Jit/code_allocator_iface.h b/cinderx/Jit/code_allocator_iface.h
index 1d2b1fd2d48bd9f1df9e44ef44efda839cf6c1f1..3e612fa6891b7c2ff1fd9047044ec7e50eb783d8 100644
--- a/cinderx/Jit/code_allocator_iface.h
+++ b/cinderx/Jit/code_allocator_iface.h
@@ -29,6 +29,11 @@ class ICodeAllocator {
   // Check if a pointer is located within this allocator's memory.
   virtual bool contains(const void* ptr) const = 0;
 
+  // Translate an executable address owned by this allocator to the
+  // corresponding writable alias. JIT code must never be writable through
+  // its executable address.
+  virtual void* writableAddress(const void* executable) const = 0;
+
   // Get the number of bytes of memory used by the allocator.
   virtual size_t usedBytes() const = 0;
 
diff --git a/cinderx/Jit/code_patcher.cpp b/cinderx/Jit/code_patcher.cpp
index d07573c9527982249e33de03d48d2c7ee710593d..8501d8e3d74432e5cc43a8af0bebd8ac48e8a68e 100644
--- a/cinderx/Jit/code_patcher.cpp
+++ b/cinderx/Jit/code_patcher.cpp
@@ -4,7 +4,9 @@
 
 #include "cinderx/Common/log.h"
 #include "cinderx/Common/util.h"
+#include "cinderx/Jit/code_allocator_iface.h"
 #include "cinderx/Jit/codegen/arch/detection.h"
+#include "cinderx/module_state.h"
 
 #include <array>
 #include <cstring>
@@ -66,6 +68,22 @@ uintptr_t resolveDisplacement(uintptr_t from, int32_t displacement) {
   return disp;
 }
 
+uint8_t* writablePatchpoint(uint8_t* executable) {
+  ICodeAllocator* allocator = cinderx::getModuleState()->code_allocator.get();
+  JIT_CHECK(allocator != nullptr, "JIT code allocator is not initialized");
+  auto* writable =
+      static_cast<uint8_t*>(allocator->writableAddress(executable));
+  JIT_CHECK(
+      writable != nullptr,
+      "Patchpoint {} is outside the JIT code allocator",
+      static_cast<void*>(executable));
+  JIT_CHECK(
+      writable != executable,
+      "Patchpoint {} does not have a distinct writable alias",
+      static_cast<void*>(executable));
+  return writable;
+}
+
 } // namespace
 
 void CodePatcher::link(uintptr_t patchpoint, std::span<const uint8_t> data) {
@@ -126,6 +144,7 @@ void CodePatcher::swap() {
 #ifdef Py_GIL_DISABLED
   SwapLockGuard lock{*this};
 #endif
+  uint8_t* writable_patchpoint = writablePatchpoint(patchpoint_);
 
 #if defined(CINDER_X86_64) && defined(Py_GIL_DISABLED)
   // On x86 the patchpoint is up to 7 bytes (aligned to 8 bytes by the code
@@ -141,12 +160,13 @@ void CodePatcher::swap() {
   auto* qword_bytes = reinterpret_cast<uint8_t*>(&qword);
   std::swap_ranges(qword_bytes, qword_bytes + flags_.data_len, data_.data());
 
-  std::atomic_ref<uint64_t>{*reinterpret_cast<uint64_t*>(patchpoint_)}.store(
-      qword, std::memory_order_relaxed);
+  std::atomic_ref<uint64_t>{
+      *reinterpret_cast<uint64_t*>(writable_patchpoint)}
+      .store(qword, std::memory_order_relaxed);
 #else
   decltype(data_) temp;
   std::memcpy(temp.data(), patchpoint_, flags_.data_len);
-  std::memcpy(patchpoint_, data_.data(), flags_.data_len);
+  std::memcpy(writable_patchpoint, data_.data(), flags_.data_len);
   std::memcpy(data_.data(), temp.data(), flags_.data_len);
 #endif
 
diff --git a/cinderx/RuntimeTests/code_allocator_test.cpp b/cinderx/RuntimeTests/code_allocator_test.cpp
index fb43355d2f853f6a00d63f9006f6a7441f5588e6..b755934514aa0cf29eace36b1e4f9fc907fcafa5 100644
--- a/cinderx/RuntimeTests/code_allocator_test.cpp
+++ b/cinderx/RuntimeTests/code_allocator_test.cpp
@@ -10,7 +10,14 @@
 #include <array>
 #include <cstdint>
 #include <cstring>
+#include <fstream>
 #include <memory>
+#include <string>
+
+#ifndef WIN32
+#include <sys/wait.h>
+#include <unistd.h>
+#endif
 
 using namespace jit;
 using namespace jit::codegen;
@@ -40,6 +47,31 @@ const uint8_t* addressAtOffset(const void* base, uintptr_t offset) {
       reinterpret_cast<uintptr_t>(base) + offset);
 }
 
+std::string permissionsFor(const void* address) {
+#ifndef WIN32
+  std::ifstream maps{"/proc/self/maps"};
+  std::string range;
+  std::string permissions;
+  auto target = reinterpret_cast<uintptr_t>(address);
+  while (maps >> range >> permissions) {
+    auto separator = range.find('-');
+    if (separator == std::string::npos) {
+      continue;
+    }
+    uintptr_t start = std::stoull(range.substr(0, separator), nullptr, 16);
+    uintptr_t end = std::stoull(range.substr(separator + 1), nullptr, 16);
+    if (start <= target && target < end) {
+      return permissions;
+    }
+    std::string remainder;
+    std::getline(maps, remainder);
+  }
+#else
+  (void)address;
+#endif
+  return {};
+}
+
 TEST_F(CodeAllocatorTest, AddSplitCodeCopiesHotAndColdSections) {
   asmjit::CodeHolder code;
   code.init(code_allocator_->asmJitEnvironment());
@@ -87,6 +119,86 @@ TEST_F(CodeAllocatorTest, AddSplitCodeCopiesHotAndColdSections) {
           kColdText.data(),
           kColdText.size()),
       0);
+
+  void* writable = code_allocator_->writableAddress(result.addr);
+  ASSERT_NE(writable, nullptr);
+  ASSERT_NE(writable, result.addr);
+#ifndef WIN32
+  EXPECT_EQ(permissionsFor(result.addr).substr(0, 3), "r-x");
+  EXPECT_EQ(permissionsFor(writable).substr(0, 3), "rw-");
+#endif
+
+  CodeAllocator standard_allocator;
+  asmjit::CodeHolder standard_code;
+  ASSERT_EQ(
+      standard_code.init(standard_allocator.asmJitEnvironment()),
+      asmjit::kErrorOk);
+  arch::Builder standard_assembler{&standard_code};
+  ASSERT_EQ(
+      standard_assembler.embed(kHotText.data(), kHotText.size()),
+      asmjit::kErrorOk);
+  ASSERT_EQ(standard_assembler.finalize(), asmjit::kErrorOk);
+
+  AllocateResult standard_result = standard_allocator.addCode(&standard_code);
+  ASSERT_EQ(standard_result.error, asmjit::kErrorOk);
+  ASSERT_NE(standard_result.addr, nullptr);
+  void* standard_writable =
+      standard_allocator.writableAddress(standard_result.addr);
+  ASSERT_NE(standard_writable, nullptr);
+  ASSERT_NE(standard_writable, standard_result.addr);
+#ifndef WIN32
+  EXPECT_EQ(permissionsFor(standard_result.addr).substr(0, 3), "r-x");
+  EXPECT_EQ(permissionsFor(standard_writable).substr(0, 3), "rw-");
+
+  pid_t child = fork();
+  ASSERT_NE(child, -1);
+  if (child == 0) {
+    asmjit::CodeHolder child_huge_code;
+    asmjit::CodeHolder child_standard_code;
+    if (
+        child_huge_code.init(code_allocator_->asmJitEnvironment()) !=
+            asmjit::kErrorOk ||
+        child_standard_code.init(standard_allocator.asmJitEnvironment()) !=
+            asmjit::kErrorOk) {
+      _exit(1);
+    }
+    arch::Builder child_huge_assembler{&child_huge_code};
+    arch::Builder child_standard_assembler{&child_standard_code};
+    bool assembled =
+        child_huge_assembler.embed(kHotText.data(), kHotText.size()) ==
+            asmjit::kErrorOk &&
+        child_huge_assembler.finalize() == asmjit::kErrorOk &&
+        child_standard_assembler.embed(kHotText.data(), kHotText.size()) ==
+            asmjit::kErrorOk &&
+        child_standard_assembler.finalize() == asmjit::kErrorOk;
+    AllocateResult child_huge_result =
+        code_allocator_->addCode(&child_huge_code);
+    AllocateResult child_standard_result =
+        standard_allocator.addCode(&child_standard_code);
+    bool added =
+        child_huge_result.error == asmjit::kErrorOk &&
+        child_huge_result.addr != nullptr &&
+        child_standard_result.error == asmjit::kErrorOk &&
+        child_standard_result.addr != nullptr;
+    bool isolated =
+        added &&
+        code_allocator_->writableAddress(result.addr) == nullptr &&
+        standard_allocator.writableAddress(standard_result.addr) == nullptr &&
+        code_allocator_->writableAddress(child_huge_result.addr) != nullptr &&
+        standard_allocator.writableAddress(child_standard_result.addr) !=
+            nullptr;
+    _exit(
+        assembled &&
+            added &&
+            isolated
+        ? 0
+        : 1);
+  }
+  int status = 0;
+  ASSERT_EQ(waitpid(child, &status, 0), child);
+  ASSERT_TRUE(WIFEXITED(status));
+  EXPECT_EQ(WEXITSTATUS(status), 0);
+#endif
 }
 
 } // namespace
diff --git a/cinderx/RuntimeTests/deopt_patcher_test.cpp b/cinderx/RuntimeTests/deopt_patcher_test.cpp
index 99971f26f0b96f39dd209fd883df376eb545da7c..badbe007ee7ab9382f1f20ac288c12c942639055 100644
--- a/cinderx/RuntimeTests/deopt_patcher_test.cpp
+++ b/cinderx/RuntimeTests/deopt_patcher_test.cpp
@@ -2,6 +2,7 @@
 
 #include <gtest/gtest.h>
 
+#include "cinderx/Jit/codegen/arch.h"
 #include "cinderx/Jit/codegen/gen_asm.h"
 #include "cinderx/Jit/compiler.h"
 #include "cinderx/Jit/context.h"
@@ -9,6 +10,7 @@
 #include "cinderx/Jit/hir/hir.h"
 #include "cinderx/Jit/hir/printer.h"
 #include "cinderx/RuntimeTests/fixtures.h"
+#include "cinderx/module_state.h"
 
 class CodePatcherTest : public RuntimeTest {
  public:
@@ -72,10 +74,27 @@ class MyDeoptPatcher : public jit::JumpPatcher {
 };
 
 TEST_F(CodePatcherTest, CodePatch) {
-  // Intentionally leaving these together to catch accidental stack scribbling.
-  uint16_t x = 123;
-  uint16_t y = 456;
-  uint16_t z = 789;
+  // Intentionally leave these together in allocator-owned code memory to
+  // catch accidental scribbling while exercising the real RX/RW aliases.
+  constexpr std::array<uint16_t, 3> values{123, 456, 789};
+  auto* allocator = cinderx::getModuleState()->code_allocator.get();
+  ASSERT_NE(allocator, nullptr);
+
+  asmjit::CodeHolder code;
+  ASSERT_EQ(code.init(allocator->asmJitEnvironment()), asmjit::kErrorOk);
+  jit::codegen::arch::Builder assembler{&code};
+  ASSERT_EQ(
+      assembler.embed(values.data(), sizeof(values)), asmjit::kErrorOk);
+  ASSERT_EQ(assembler.finalize(), asmjit::kErrorOk);
+
+  jit::AllocateResult allocation = allocator->addCode(&code);
+  ASSERT_EQ(allocation.error, asmjit::kErrorOk);
+  ASSERT_NE(allocation.addr, nullptr);
+  auto* allocated_values =
+      static_cast<const uint16_t*>(allocation.addr);
+  const uint16_t& x = allocated_values[0];
+  const uint16_t& y = allocated_values[1];
+  const uint16_t& z = allocated_values[2];
 
   std::array<uint8_t, 2> bytes{0xef, 0xbe};
 

