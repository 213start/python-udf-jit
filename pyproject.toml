[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "python-udf-jit"
version = "0.1.0"
description = "Transparent scalar Python UDF JIT piercing"
requires-python = ">=3.14"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
